// TODO (Dev C — Phase 2): Replace stub with full implementation
// Layout: sandbox sliders (4 adjustments) + new WWS preview + Monte Carlo fan chart
// Ref: CLAUDE.md Section 11, Phase 2 / Dev C

export default function TreatmentPlan() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-text-muted">Treatment Plan — coming in Phase 2</p>
    </div>
  )
}
