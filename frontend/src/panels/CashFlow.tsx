// TODO (Dev B — Phase 2): Replace stub with full implementation
// Layout: KPI row + savings rate gauge + expense BarChart/PieChart + monthly trend LineChart
// Ref: CLAUDE.md Section 11, Phase 2 / Dev B

export default function CashFlow() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-text-muted">Cash Flow panel — coming in Phase 2</p>
    </div>
  )
}
