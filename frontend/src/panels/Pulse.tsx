// TODO (Dev B — Phase 2): Replace stub with full implementation
// Layout: ScoreDial (left) + VitalBar list (right) + net worth AreaChart (bottom)
// Ref: CLAUDE.md Section 11, Phase 2 / Dev B

export default function Pulse() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-text-muted">Pulse panel — coming in Phase 2</p>
    </div>
  )
}
