// TODO (Dev C — Phase 2): Replace stub with full implementation
// Layout: 3× RxCard + sentiment selector + prompt chips + chat interface
// Ref: CLAUDE.md Section 11, Phase 2 / Dev C

export default function PrescriptionPad() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-text-muted">Prescription Pad — coming in Phase 2</p>
    </div>
  )
}
