// TODO (Dev B — Phase 2): Replace stub with full implementation
// Layout: RadarChart (5 axes) + 8-metric scorecard grid
// Ref: CLAUDE.md Section 11, Phase 2 / Dev B

export default function Vitals() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-text-muted">Vitals panel — coming in Phase 2</p>
    </div>
  )
}
